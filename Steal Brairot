-- Steal a Brainrot — ESP + Infinity Jump
if _G.SAB_LOADED then print("[SAB] Already loaded!"); return end
_G.SAB_LOADED = true

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local Config = {
    ESP         = true,
    InfJump     = false,
}

-- GUI Parent
local guiParent
pcall(function()
    local cg = game:GetService("CoreGui")
    local t = Instance.new("ScreenGui"); t.Parent = cg; t:Destroy()
    guiParent = cg
end)
if not guiParent then guiParent = LocalPlayer:WaitForChild("PlayerGui") end
local old = guiParent:FindFirstChild("SAB_UI"); if old then old:Destroy() end

-- GUI
local SG = Instance.new("ScreenGui")
SG.Name="SAB_UI"; SG.ResetOnSpawn=false; SG.Parent=guiParent

local Frame = Instance.new("Frame",SG)
Frame.Size=UDim2.new(0,220,0,160); Frame.Position=UDim2.new(0,20,0.5,-80)
Frame.BackgroundColor3=Color3.fromRGB(12,12,18); Frame.BorderSizePixel=0
Frame.Active=true; Frame.Draggable=true
Instance.new("UICorner",Frame).CornerRadius=UDim.new(0,8)
local fst=Instance.new("UIStroke",Frame); fst.Color=Color3.fromRGB(100,60,140); fst.Thickness=1.2

local TB=Instance.new("Frame",Frame); TB.Size=UDim2.new(1,0,0,34)
TB.BackgroundColor3=Color3.fromRGB(24,16,40); TB.BorderSizePixel=0
Instance.new("UICorner",TB).CornerRadius=UDim.new(0,8)
local TFix=Instance.new("Frame",TB); TFix.Size=UDim2.new(1,0,0,10); TFix.Position=UDim2.new(0,0,1,-10)
TFix.BackgroundColor3=Color3.fromRGB(24,16,40); TFix.BorderSizePixel=0
local TL=Instance.new("TextLabel",TB); TL.Size=UDim2.new(1,-10,1,0); TL.Position=UDim2.new(0,12,0,0)
TL.BackgroundTransparency=1; TL.Text="🧠  Steal a Brainrot"
TL.TextColor3=Color3.fromRGB(210,180,255); TL.TextSize=13
TL.Font=Enum.Font.GothamBold; TL.TextXAlignment=Enum.TextXAlignment.Left

local pad=Instance.new("UIPadding",Frame)
pad.PaddingTop=UDim.new(0,42); pad.PaddingLeft=UDim.new(0,12)
pad.PaddingRight=UDim.new(0,12); pad.PaddingBottom=UDim.new(0,12)
local layout=Instance.new("UIListLayout",Frame)
layout.SortOrder=Enum.SortOrder.LayoutOrder; layout.Padding=UDim.new(0,10)

local function makeToggle(text,order,configKey)
    local row=Instance.new("Frame",Frame); row.Size=UDim2.new(1,0,0,28)
    row.BackgroundTransparency=1; row.LayoutOrder=order
    local lbl=Instance.new("TextLabel",row); lbl.Size=UDim2.new(0.65,0,1,0)
    lbl.BackgroundTransparency=1; lbl.Text=text; lbl.TextColor3=Color3.fromRGB(210,200,255)
    lbl.TextSize=12; lbl.Font=Enum.Font.GothamSemibold; lbl.TextXAlignment=Enum.TextXAlignment.Left
    local btn=Instance.new("TextButton",row); btn.Size=UDim2.new(0,44,0,22)
    btn.Position=UDim2.new(1,-44,0.5,-11); btn.BackgroundColor3=Color3.fromRGB(50,40,70)
    btn.Text=""; btn.AutoButtonColor=false
    Instance.new("UICorner",btn).CornerRadius=UDim.new(1,0)
    local circle=Instance.new("Frame",btn); circle.Size=UDim2.new(0,16,0,16)
    circle.Position=UDim2.new(0,3,0.5,-8); circle.BackgroundColor3=Color3.fromRGB(130,120,160)
    Instance.new("UICorner",circle).CornerRadius=UDim.new(1,0)
    local function refresh()
        if Config[configKey] then
            btn.BackgroundColor3=Color3.fromRGB(120,60,220)
            circle.BackgroundColor3=Color3.fromRGB(255,255,255)
            circle:TweenPosition(UDim2.new(1,-19,0.5,-8),"Out","Quad",0.12,true)
        else
            btn.BackgroundColor3=Color3.fromRGB(50,40,70)
            circle.BackgroundColor3=Color3.fromRGB(130,120,160)
            circle:TweenPosition(UDim2.new(0,3,0.5,-8),"Out","Quad",0.12,true)
        end
    end
    refresh()
    btn.MouseButton1Click:Connect(function() Config[configKey]=not Config[configKey]; refresh() end)
end

makeToggle("ESP  [E]",          1, "ESP")
makeToggle("Infinity Jump  [J]", 2, "InfJump")

-- ================================================
--  ESP — แสดงชื่อ + ไฮไลท์ผ่านกำแพง
-- ================================================
local espData = {}

local function removeESP(p)
    local d = espData[p]; if not d then return end
    pcall(function() d.hl:Destroy() end)
    pcall(function() d.bb:Destroy() end)
    espData[p] = nil
end

local function updateESP(p)
    if not Config.ESP then removeESP(p); return end
    local char = p.Character; if not char then removeESP(p); return end
    local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
    local hum  = char:FindFirstChildOfClass("Humanoid")
    if hum and hum.Health <= 0 then removeESP(p); return end

    local d = espData[p]
    if not d then
        local hl = Instance.new("Highlight")
        hl.FillTransparency = 0.5
        hl.OutlineTransparency = 0
        hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop

        local bb = Instance.new("BillboardGui")
        bb.Size = UDim2.new(0, 120, 0, 40)
        bb.StudsOffset = Vector3.new(0, 3.2, 0)
        bb.AlwaysOnTop = true

        local nameLbl = Instance.new("TextLabel", bb)
        nameLbl.Size = UDim2.new(1,0,0.6,0)
        nameLbl.BackgroundTransparency = 1
        nameLbl.Text = p.Name
        nameLbl.TextColor3 = Color3.fromRGB(255,255,255)
        nameLbl.TextSize = 13; nameLbl.Font = Enum.Font.GothamBold
        nameLbl.TextStrokeTransparency = 0.3
        nameLbl.TextStrokeColor3 = Color3.fromRGB(0,0,0)

        local distLbl = Instance.new("TextLabel", bb)
        distLbl.Size = UDim2.new(1,0,0.4,0)
        distLbl.Position = UDim2.new(0,0,0.6,0)
        distLbl.BackgroundTransparency = 1
        distLbl.TextSize = 10; distLbl.Font = Enum.Font.Gotham
        distLbl.TextColor3 = Color3.fromRGB(200,180,255)
        distLbl.TextStrokeTransparency = 0.3
        distLbl.TextStrokeColor3 = Color3.fromRGB(0,0,0)

        d = {hl=hl, bb=bb, distLbl=distLbl}
        espData[p] = d
    end

    if d.hl.Parent ~= char then d.hl.Adornee = char; d.hl.Parent = char end
    if d.bb.Parent ~= root then d.bb.Adornee = root; d.bb.Parent = root end

    -- สี highlight ตาม team (ถ้ามี)
    local myTeam = LocalPlayer.Team
    if p.Team and myTeam and p.Team == myTeam then
        d.hl.FillColor = Color3.fromRGB(50,150,255)   -- น้ำเงิน = teammate
        d.hl.OutlineColor = Color3.fromRGB(100,200,255)
    else
        d.hl.FillColor = Color3.fromRGB(220,50,50)    -- แดง = ศัตรู
        d.hl.OutlineColor = Color3.fromRGB(255,255,255)
    end

    -- ระยะห่าง
    local myChar = LocalPlayer.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if myRoot then
        local dist = math.floor((root.Position - myRoot.Position).Magnitude)
        d.distLbl.Text = "📍 " .. dist .. " studs"
    end
end

-- ================================================
--  INFINITY JUMP
-- ================================================
local infJumpConn = nil
local function setupInfJump()
    if infJumpConn then infJumpConn:Disconnect(); infJumpConn = nil end
    if not Config.InfJump then return end

    local char = LocalPlayer.Character; if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid"); if not hum then return end

    infJumpConn = hum.StateChanged:Connect(function(_, new)
        if Config.InfJump and new == Enum.HumanoidStateType.Freefall then
            task.wait(0.1)
            hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
        end
    end)
end

UserInputService.JumpRequest:Connect(function()
    if not Config.InfJump then return end
    local char = LocalPlayer.Character; if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid"); if not hum then return end
    if hum:GetState() == Enum.HumanoidStateType.Freefall then
        hum:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- setup ใหม่ทุกครั้งที่ respawn
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    setupInfJump()
end)
setupInfJump()

-- ================================================
--  MAIN LOOP
-- ================================================
RunService.RenderStepped:Connect(function()
    -- อัป ESP ทุก player
    for _, p in ipairs(Players:GetPlayers()) do
        if p == LocalPlayer then continue end
        updateESP(p)
    end

    -- sync InfJump กับ character ใหม่
    if Config.InfJump and not infJumpConn then
        setupInfJump()
    end
end)

Players.PlayerRemoving:Connect(removeESP)

-- ================================================
--  KEYBINDS
-- ================================================
UserInputService.InputBegan:Connect(function(inp, gp)
    if gp then return end
    if inp.KeyCode == Enum.KeyCode.E then
        Config.ESP = not Config.ESP
        print("[SAB] ESP = " .. tostring(Config.ESP))
    end
    if inp.KeyCode == Enum.KeyCode.J then
        Config.InfJump = not Config.InfJump
        if Config.InfJump then setupInfJump() end
        print("[SAB] InfJump = " .. tostring(Config.InfJump))
    end
end)

print("[SAB] Loaded! E = ESP | J = Infinity Jump")
